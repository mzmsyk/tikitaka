using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class okyonu : MonoBehaviour
{
    // Start is called before the first frame update
    public Transform okYonu;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
